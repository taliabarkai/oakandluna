/**
 * Sellable product tiles on the demo category PLP (excludes Editorial / Lookbook image-only tiles).
 * Keep in sync with {@link CategoryPageLayout} `<ProductCard />` count.
 */
export const CATEGORY_PLP_PRODUCT_COUNT = 24;
